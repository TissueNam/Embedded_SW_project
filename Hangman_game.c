#include <stdio.h>
#include <string.h>
int play(char* txt);

int main()
{
	while(1)
	{
		printf("Waiting for game participation...\n");
		sleep(3);
	}
	return 0;
}

int play(char* txt)
{
	char buf[100] = {0,};
	// txt
	if(strncmp())
}
